// Karen Adventures — PostgreSQL schema
// Premium Kenyan travel, adventure & curated experiences.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

/// A curated journey / safari / adventure offered by Karen Adventures.
model Adventure {
  id            String   @id @default(cuid())
  slug          String   @unique
  title         String
  tagline       String
  description   String
  image         String
  imageAlt      String
  duration      String
  startingPrice Int // USD, per person
  location      String
  region        String
  tripType      String // Safari | Adventure | Coastal | Cultural | Luxury | Expedition
  bestSeason    String?
  groupSize     String?
  featured      Boolean  @default(false)
  highlights    String[]
  itinerary     Json? // day-by-day: [{ day, title, description }]
  inclusions    String[] @default([])
  exclusions    String[] @default([])
  accommodation String?
  transport     String?
  createdAt     DateTime @default(now())

  featuredIn Destination[] @relation("DestinationTrips")

  @@index([featured])
  @@index([tripType])
}

/// A destination on the Explorer map (East Africa).
model Destination {
  id              String   @id @default(cuid())
  slug            String   @unique
  name            String
  region          String
  country         String   @default("Kenya")
  description     String
  image           String
  imageAlt        String
  latitude        Float
  longitude       Float
  bestExperiences String[]
  images          String[] @default([]) // gallery (first entry = hero)
  recommendedTrips Adventure[] @relation("DestinationTrips")
  createdAt       DateTime @default(now())
}

/// A signature experience category (Private, Group, Luxury, etc.).
model Experience {
  id          String @id @default(cuid())
  slug        String @unique
  title       String
  description String
  icon        String // lucide icon name
  order       Int    @default(0)
}

/// Guest stories.
model Testimonial {
  id       String @id @default(cuid())
  name     String
  location String
  tripType String
  quote    String
  rating   Int    @default(5)
}

/// Editorial gallery imagery.
model GalleryItem {
  id       String @id @default(cuid())
  image    String
  alt      String
  category String // Safari | Landscape | Coast | Culture | Escape
  width    Int
  height   Int
  order    Int    @default(0)
}

/// Booking & planning inquiries from the contact form.
model Inquiry {
  id          String   @id @default(cuid())
  name        String
  email       String
  phone       String?
  destination String?
  tripType    String?
  travelers   String?
  travelDate  String?
  message     String
  status      String   @default("new") // new | planned | confirmed | archived
  notes       String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([status])
  @@index([createdAt])
}

/// A reserved journey — created by guests via the public booking flow.
model Booking {
  id             String    @id @default(cuid())
  reference      String    @unique
  name           String
  email          String
  phone          String?
  adventureSlug  String?
  adventureTitle String
  destination    String?
  destinations   String[]  @default([]) // all locations a guest wants to visit
  travelers      Int
  adults         Int       @default(1)
  children       Int       @default(0)
  startDate      DateTime?
  endDate        DateTime?
  pickupLocation String?
  pickupTime     String?
  dropoffLocation String?
  dropoffTime    String?
  accommodation  String?
  transport      String?
  depositPaidKes Int? // deposit actually received via Paystack (KES)
  priceEstimate  Int
  status         String    @default("pending") // pending | confirmed | completed | cancelled
  notes          String?
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt

  @@index([status])
  @@index([createdAt])
}

/// Newsletter subscribers from the footer.
model Subscriber {
  id        String   @id @default(cuid())
  email     String   @unique
  createdAt DateTime @default(now())
}
